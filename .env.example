# Provider and Model Configuration
# Choose your default provider: "LM Studio" or "OpenAI"
DEFAULT_PROVIDER="LM Studio"

# --- OpenAI Configuration ---
# Required if DEFAULT_PROVIDER is "OpenAI" or if you plan to use OpenAI
OPENAI_API_KEY="your_openai_api_key_here"
OPENAI_MODEL_ID="gpt-4o"

# --- LM Studio Configuration ---
# Optional: Specify a default model key for LM Studio.
# If empty, you will be prompted to choose from downloaded models.
LM_STUDIO_MODEL_KEY=""
# Optional: Specify context length for LM Studio models.
LM_STUDIO_CONTEXT_LENGTH="32768"

# --- Logging Configuration ---
# Enable/disable verbose logging in the CLI (true/false)
# When disabled, only errors and warnings are shown in CLI
# All logs are always saved to the logs/ directory regardless of this setting
VERBOSE_LOGGING="false"
